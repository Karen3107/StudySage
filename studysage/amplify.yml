version: 1
applications:
  - appRoot: studysage
    frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: studysage/build
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
